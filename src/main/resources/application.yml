spring:
  jpa:
    hibernate:
      ddl-auto: validate
      dialect: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:postgresql://localhost:5433/sample
    username: sakthi
    password: sakthi

  mail:
    host: smtp.gmail.com
    port: 587
    username: spriyan2000@gmail.com
    password: fmwfyovdzxhiidkk
    default-encoding: UTF-8
    properties:
      mail:
        mime:
          charset: UTF
        smtp:
          writetimeout: 10000
          connectiontimeout: 10000
          timeout: 10000
          auth: true
          starttls:
            enable: true
            required: true
    verify:
      host: http://localhost:8080